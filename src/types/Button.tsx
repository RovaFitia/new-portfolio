type ButtonProps = {
    variant?: 'solid' | 'ghost' | 'super';
    size?: 'md' | 'lg';
    className?: string;
    children: React.ReactNode;
    href?: string;
    icon?: boolean;
};

export type { ButtonProps };
